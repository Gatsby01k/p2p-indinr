node_modules/
.next/
out/
build/
dist/
coverage/
*.tsbuildinfo
next-env.d.ts
.env*
!.env.example
.DS_Store

# Sandbox database (embedded PostgreSQL data dir + runtime state).
# The gate and the rehearsal each run their own isolated cluster in a
# sibling directory, so the pattern covers every `SANDBOX_PG_DIR`.
.sandbox-db*/

# DEL-10 evidence artefacts: generated by the drills and the browser run,
# kept out of the source tree so a screenshot never becomes a commit.
/artifacts/
/sbom.json
/MIGRATION-CONVERGENCE.json
